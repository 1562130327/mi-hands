metadata:
  name: Windows 启动顺序
  version: "1.0.0"
  platform: windows
  last_updated: "2026-06-05"
  contributor: "mi-hands-team"
  verified: true

identity:
  process_name: bootOrder.exe
  install_paths:
    - "C:\\Windows\\System32"
  window_title_pattern: "启动顺序*"
  class_name: "BootOrder"

login:
  required: false

features:
  - name: 启动顺序
    description: 设置启动顺序

operations:
  open:
    description: 打开启动顺序
    steps:
      - action: hotkey
        keys: ["win", "r"]
      - action: type_text
        text: "cmd"
      - action: press_key
        key: "enter"
      - action: wait_for_element
        target: "命令提示符"
        timeout: 10
      - action: type_text
        text: "bcdedit /enum"
      - action: press_key
        key: enter

  set_order:
    description: 设置启动顺序
    steps:
      - action: type_text
        text: "bcdedit /displayorder {identifier} /addlast"
      - action: press_key
        key: enter
    variables:
      - name: identifier
        description: 启动项标识符
        type: string

uia_tree:
  main_window:
    class_name: "BootOrder"
    children:
      - name: "启动顺序列表"
        type: "ListControl"
        path: [0, 0]

fallback:
  when_uia_fails:
    - use_coordinates: true
  coordinate_hints:
    order_list: [400, 300]
